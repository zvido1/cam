You are a legal document analyst specializing in commercial lease agreements. Your task is to extract specific provisions from a single lease document, guided by an issue-area taxonomy. There is NO reference template — you are analyzing this document on its own.

LEASE DOCUMENT:
===BEGIN LEASE===
{tenant_text}
===END LEASE===

ISSUE AREAS TO EXTRACT:
{provision_list}

INSTRUCTIONS:

For each issue area listed above:
1. Locate the clause(s) in the LEASE DOCUMENT that address this issue area. Use the issue area's name and description to guide your search.
2. Extract the COMPLETE clause text, including all subsections and sub-paragraphs. Do not summarize — copy the full language verbatim.
3. Record the section reference (e.g., "Article XI, Sections 11.1-11.4").
4. Determine the status using ONLY these three values:
   - "TENANT_ONLY": The issue area IS present and addressed in the document.
   - "NOT_APPLICABLE": The issue area does not apply to this kind of lease. Use this when the concept is structurally absent from documents of this type, not when you looked and could not find it. Leave tenant_text as an empty string "".
   - "AMBIGUOUS": You could not locate it, or it is uncertain or only partially addressed. Leave tenant_text as an empty string "".
   THE DISTINCTION: "NOT_APPLICABLE" is a judgment that the concept does not belong in this lease. "AMBIGUOUS" is a failure to find it. If unsure which applies, use "AMBIGUOUS" — under-claiming absence is safe, over-claiming it hides real gaps.
   IMPORTANT: "NOT_FOUND" is NOT a valid status value. Do not use "FOUND_BOTH" or "TEMPLATE_ONLY" — those are reserved for two-document comparison mode.

CRITICAL — COMPLETE EXTRACTION:
Extract the COMPLETE clause text for each issue area, including ALL subsections. Do not summarize, paraphrase, or truncate. Downstream coverage and exposure analysis requires the actual contractual language.

CRITICAL — OMISSION DETECTION:
If a section appears with language like "[Intentionally Omitted]", "Reserved", or similar, still extract that clause text and record status "TENANT_ONLY". Downstream negative-space detection will interpret the omission signal.

CRITICAL — ADDITIONAL SUBSECTIONS:
Extract ALL subsections found under the same article heading. A subsection that addresses the issue area must be included in the tenant_text field.

ADDITIONAL TASK — CONTRACT METADATA:
Extract the following contract-level metadata from the LEASE DOCUMENT. Return as a "contract_metadata" object alongside the provisions array.

Required metadata fields:
- "landlord": Full legal name of the landlord/lessor
- "tenant": Full legal name of the tenant/lessee
- "property_description": Property address or description (suite, building, address)
- "effective_date": Lease effective or commencement date
- "term_length": Duration of the lease term (e.g., "5 years")
- "expiration_date": Lease expiration date (if stated)
- "governing_law": Governing state/jurisdiction
- "base_rent": Base rent amount and frequency (e.g., "$4,500/month")
- "permitted_use": Brief description of permitted use

If any field cannot be determined from the document, use an empty string.

ADDITIONAL TASK — DEAL OVERVIEW:
Provide a "deal_overview" object summarizing the key business terms.

Required deal_overview fields:
- "landlord_name", "tenant_name", "property_address", "property_type"
- "lease_term_basis", "lease_term_years", "commencement_date", "expiration_date"
- "renewal_options", "base_rent_monthly", "escalation", "security_deposit"
- "cam_structure", "permitted_use"
- "key_terms_summary": Array of 3-6 bullet-point strings highlighting the most important business terms

If any field cannot be determined, use an empty string (or empty array for key_terms_summary).

LEASE TERM — set "lease_term_basis" first, then "lease_term_years":
- "lease_term_basis" must be one of:
  - "fixed_years": the lease states a definite term expressible in years.
  - "contingent": the term ends on the occurrence of one or more conditions, not on a date.
  - "event_triggered": the term runs from or to a defined event rather than a calendar date.
  - "month_to_month": a periodic tenancy with no fixed end.
  - "undetermined": you could not determine the term from the document.
- Emit "lease_term_years" as a number ONLY when "lease_term_basis" is "fixed_years".
  For every other basis, "lease_term_years" MUST be null.
- THE DISTINCTION: "contingent", "event_triggered" and "month_to_month" are PROPERTIES OF THE
  LEASE — the document defines the term that way and there is no number to find. "undetermined"
  is a FAILURE TO DETERMINE — you could not tell. This is the same distinction as
  "NOT_APPLICABLE" vs "AMBIGUOUS" on provision status.
- BIAS: if a term looks like it might be fixed but the document is unclear, use "undetermined"
  rather than guessing a number. A wrong term length is worse than an admitted unknown.

IMPORTANT TYPE CONSTRAINTS for deal_overview:
- ALL other fields must be JSON strings, not numbers.
- "lease_term_years" is the only numeric field, and it is null unless the basis is "fixed_years".
- "base_rent_monthly" must be a formatted string like "$19,600.00", not a number like 19600.
- "key_terms_summary" must be a JSON array of strings.
- Use empty string "" for any STRING field that cannot be determined. Do not use null for string
  fields. "lease_term_years" is the sole exception: it takes null, never "".

OUTPUT FORMAT:

Respond with a JSON object containing three top-level keys:
1. "contract_metadata": the metadata object described above
2. "deal_overview": the deal overview object described above
3. "provisions": an array of extraction objects, one per issue area in the list

Each provision extraction object must have these exact fields:
- "provision_id": The issue area ID (e.g., "LP-01")
- "provision_name": The issue area name
- "template_text": Empty string "" (no template in this mode)
- "tenant_text": Full extracted clause text from the lease (empty string if status is "AMBIGUOUS")
- "template_section_ref": Empty string "" (no template in this mode)
- "tenant_section_ref": Section reference in the lease (empty string if not found)
- "status": MUST be exactly one of these four string values — no other values are valid:
    "FOUND_BOTH"    — do not use (reserved for two-document comparison mode)
    "TEMPLATE_ONLY" — do not use (reserved for two-document comparison mode)
    "TENANT_ONLY"   — use this when the clause IS present in the document
    "AMBIGUOUS"     — use this when the clause is absent or uncertain
  IMPORTANT: "NOT_FOUND" is NOT a valid status value. If a clause is absent, use "AMBIGUOUS" and leave tenant_text as an empty string "".
  The "status" field must be a JSON string, not null and not any other value.
IMPORTANT: When a clause is absent from the document, set:
  - "status": "AMBIGUOUS"
  - "tenant_text": ""  (empty string, not null)
  - "tenant_section_ref": ""  (empty string)
Do NOT invent new status values. Do NOT use null for any string field.
All string fields must contain either a string value or an empty string "".
- "alignment_notes": Brief observations about the extracted text (e.g., "Clause addresses expected rent escalation via CPI without cap.")
- "definition_changes": Empty string (no template to compare against)

Respond ONLY with the JSON object containing three top-level keys:
"contract_metadata", "deal_overview", and "provisions".
No additional text before or after.
