# CAM — New Thread Prompt
_Last updated: 2026-07-14 (411→421C arc CLOSED — evidence-assignment architectural defect found and characterized; 417/419/420 numeric baselines void; Gemini non-determinism reversal; Grok exonerated on LP-07. Next: Step 423 evidence-assignment architecture spec/implementation.)_

> **2026-07-14 UPDATE (read FIRST — newest frontier; closes the 411→421C arc; evidence-assignment defect characterized; points at Step 423).** The 411→421C arc discovered an architectural defect in Gemini's evidence assignment that voids the numeric baselines from Steps 417, 419, and 420 and reverses the earlier conclusion that Gemini extraction is deterministic. Read this block in full before starting any new work.
>
> **Current step: 423 — evidence-assignment architecture spec/implementation.** Step 422 was a QC investigation that found the defect and was STOPPED when the material diff surfaced. Step 423 is the architecture fix. The 423 architecture spec is in `build_log/423_evidence_assignment_architecture_spec.md`. The incident report is in `build_log/421C_evidence_assignment_incident.md`. Steps 422A and 422B (NOT_APPLICABLE schema hygiene and wiring) are complete as of 2026-07-14.
>
> **The open architectural defect (421C).** Gemini's extraction is destructive and exclusive. The lease is cut into 33 disjoint LP buckets; a clause is assigned to exactly one bucket and is invisible to every other LP's evaluators. The key-terms table — Tenant's Share of Operating Expenses 100%, Building's Share of Project Operating Expenses 45.79%, Rent Adjustment Percentage 3% — sits at character 1,994 in the Atreca source document. It has 0 hits across 101 Gemini-primary pipeline files and all 3 Atlas `pipeline_results.json` files. When it appears at all it lands in LP-00 (Parties & Premises), which carries `identity_check: true` in the taxonomy and is not evaluated for coverage. The evaluators assessing LP-07 Operating Expenses have never seen the parameters that define that provision's financial effect on this tenant. **Fix direction:** evidence belongs to the lease; LPs cite into it. Assignment must be many-to-many, source-traceable, and non-destructive. Non-exclusive assignment and/or a global key-terms context injection are the extraction-layer analogs of the cross-LP text injection already reduced to practice at the element layer (Supplement #05-12). Step 423 designs and/or implements this.
>
> **What is blocked until 423:** clean system-performance baseline; Stage 5 stabilization as a measurement; Priority Exposure surface. Do not cite any figure from 417, 419, or 420 as a measurement of system performance.
>
> **What survives (tested, not voided):** Step 414 (fallback/frozen-stack integrity guard, 52 tests green); Step 416 (config integrity assertion, `_check_generation_integrity()` on every evaluator call, 32 tests green); Step 421B (extraction integrity guard — fail-closed on Gemini failure, stub-provision path blocked, 65k token ceiling, evidence hashes in `pipeline_results.json`, 10 new tests green, 84/84 legacy tests green). These are structural integrity properties independent of what evidence Gemini assigns to each LP.
>
> **Void baselines (417/419/420).** The N=10 Stage 5 stability runs from Steps 417, 419, and 420 are void as system-performance baselines. The evaluators in those runs assessed a lease whose key commercial terms were structurally excluded from their evidence context. The figures — including the 81.2% overall stability figure, the 43.8% figure, and the 43–49% Grok LP-07 flip attribution — may not be cited as measurements of system performance. They remain diagnostic artifacts documenting how the defect was found. All figures must be remeasured after evidence assignment is corrected.
>
> **Gemini non-determinism reversal.** Step 421A (the initial 421 investigation, before 421B) found six byte-identical Gemini extraction outputs in the 417/419/420 runs and concluded Gemini was deterministic at the observed output length. That conclusion was an artifact of the 32,000-token ceiling — the output was truncated to the same point every run and silently repaired by `_repair_truncated_json()` into something that appeared complete. With the ceiling raised to 65,000 tokens (421B), Gemini produces two distinct extraction hashes across 15 runs: 9× `f7f64b5c` (modal) and 6× `d3e62ead` (minor), with the modal hash varying between sampling windows. **Gemini is not deterministic.** Any document stating or implying otherwise carries a voided conclusion.
>
> **Grok exonerated.** Grok's LP-07 "missing" verdicts — attributed to evaluator instability in the 417–420 arc — were correct. The quantitative parameters defining the Operating Expense mechanism were never present in Grok's evidence context. The instability measurement was measuring a panel evaluating incomplete evidence, not evaluator noise.
>
> **Standing constraints (unchanged):** no `cam/core/` epistemic changes without authorization; no `git add .` from repo root (`.tmp.driveupload` hazard); no push without preflight and sanction; stack-freeze active (A=claude-sonnet-4-6, B=gpt-5.5, C=grok-4.3 — do not swap models mid-investigation); `git add -f` required for files in `build_log/` and `Docs/` (both gitignored).

> **2026-07-08 UPDATE (read FIRST -- newest frontier; closes the 408-410 compound-consequence + reproducibility arc; re-gates Priority Exposure on Stage 5 stabilization).** **408C** (SHA `450f52d`) implemented compound-finding consequence assessment: a dedicated `_COMPOUND_FINDING_SYSTEM_PROMPT` lane (neutral, interaction-focused, not LP-consequence-reuse), `compound_`-prefixed fields on each CRX object, no LP-level contamination (LP-27 independence confirmed), forbidden-token scan PASS, 7/7 CRX assessed both Atreca runs A and B. Architecture proven sound on stable input. LP-22/LP-26 subordination-trap is the control case: consequence identical on stable compound input; instability is upstream. **409** (SHA `155c0c1`) traced CRX compound reproducibility: 0/7 CRX finding_ids stable; 1/7 semantically stable (LP-22/LP-26, Jaccard=1.00); all others diverged on LP-set or pattern-type. Priority Exposure blocked -- cannot rank findings that are redrawn each run. **410** (report: `build_log/410_stage7_candidate_instability_trace.md`; SHA `87a9fa6`, local, not pushed) staged-boundary measurement -- isolates WHERE nondeterminism enters. Three layers: **Layer 1 (PRIMARY, MEASURED)** = Stage 5 coverage-state nondeterminism. Run A flagged 18 LPs; Run B flagged 19 (LP-01/LP-15 in A only; LP-09/LP-28/LP-29 in B only). Stage 7 Pass-1 prompt hashes differ. `_build_compound_user_prompt` embeds ALL 32 LPs' coverage states so Stage 5 variance propagates directly into compound prompt content. **Layer 2 (SECONDARY, inferred)** = cross-provider LLM nondeterminism at temperature=0; already in place; not a standalone fix target. **Layer 3 (STRUCTURAL, confirmed)** = doubly-unstable CRX ordinal ID assignment: `_cluster_compound_candidates` L953 and `_dedup_compound_findings` L1661 both assign positional labels with no content-based key anywhere in the pipeline. Temperature=0 is NOT a fix. Compound sidecar gap: Step 386 instruments directional Pass-1 only; compound call has no sidecar. Fix sequence (not yet built): stabilize Stage 5 -> add content-keyed compound identity -> measure residual Layer 2 variance. **NEXT SESSION STARTS HERE (Step 411): Stage 5 Coverage Reproducibility Trace** -- read-only measurement on Atreca A/B artifacts. Which LPs change coverage state? Which elements drive the verdict crossing? Is nondeterminism concentrated or spread? Compare Stage 5 outputs element-by-element in `pipeline_results.json`. Standing caveats: all DIRECTIONAL, two leases, NOT promoted, NOT patent record; Priority Exposure blocked pending Stage 5 stabilization; widened LP-level gate stays DEFAULT-OFF.

> **2026-07-07 UPDATE (read FIRST — newest frontier; closes the 404–407 arc).** The Stage 5e sparsity investigation is RESOLVED and the Priority Exposure blocker is RE-DIAGNOSED. Chain: **404** (`a262c92`) traced `use_impact` sparsity to the deterministic `_should_assess()` gate in `cam/adapters/lease_review/lease_use_impact.py` (adapter, not frozen core) — admits missing/review_needed always, partial only at ≥50% missing elements; run-to-run set movement = upstream coverage-state wobble crossing the gate, not random selection. **405** (`9341b9a`) reconciled the prior COV record: COV-A shipped finding-level consequence for DIRECTIONAL findings via `lease_finding_consequence.py` and never touched coverage-LP `_should_assess`, so the 50% gate was deferred not decided; authorized Option A (all-partial widening) as controlled diagnostic behind a default-off flag. **406** (`8b9ed5d`) Atlas widened diagnostic, N=2 on frozen artifacts: 27/27 wide-eligible both runs (0 eligibility churn), newly-admitted yield 18/20 decisive (90%), value churn 3/27 on ambiguous clauses, chunking clean. **407** (`46fefaf`; 406 cleanup `ee5eb3a`; harness `run_407_gate2.py`) FIRST EDGAR pipeline run — Atreca EX-10.18 lab/office (cross-type vs Atlas), full Mode C ~23min × 2: transfer HELD (19/19 wide both runs, 0 eligibility churn, 0 value churn, newly-admitted 8/8 decisive 100%, no truncation/fallback). **KEY 407 FINDING:** Atreca surfaced 6 compound CRX findings (LP-01/LP-11/LP-27 interacting), ALL `not_assessed` for consequence — per-LP `use_impact` structurally cannot represent a cross-provision trap, proving LP-widening is NOT sufficient for Priority Exposure. **DECISION:** widened gate stays DEFAULT-OFF; Priority Exposure NOT built; LP-widening = validated diagnostic for coverage cards; compound/CRX finding-level consequence = the next architecture question. **NEXT SESSION STARTS HERE (408, spec fresh — do NOT reuse a tired draft): can COV-A's finding-level consequence architecture extend to compound/CRX findings, or do they need their own consequence lane?** Standing caveats: two CA tenant-side leases ≠ validation; original multi-finding-per-LP coverage-card caveat still UNTESTED (both leases 1:1); all DIRECTIONAL, NOT promoted, NOT patent record. Doctrine intact: Risk stays action-type; consequence orthogonal to buckets; Priority Exposure sits ABOVE buckets, never a Risk redefinition.

> **2026-07-05 UPDATE (read before the Current-state block below).** The 398/398b Context Dependency UI strip is SHIPPED (local, unpushed: `20f5095`, `758bdf6`), verified 2026-07-05 against the frozen run `results/lease_review_20260611_145145_3131a1` by reading the real LP-05 `use_impact` and the `buildItem()` render. It surfaces Stage 5e `use_impact` on Mode C coverage cards: an amber strip below the indigo 375G Client Impact box. Follow-on handoff `build_log/399_chat_instruction.md` (written this session) rewords the strip from machinery language ("Evaluators split 1-1-1") to plain reader-facing text ("Depends on your use" / "Genuinely unsettled — could cut either way"), decided against the REAL card. **399 BUILT by Code, commit `9d8ede2` (local, unpushed).** Doc-drift caught during 399: `398_code_status.md` quoted the 1-1-1 note as "answer depends on tenant's specific use." but the live file held "outcome depends on the specific tenant use." (an un-backported 398c apostrophe-fix). Immaterial — that whole string was being replaced — but logged as a reminder the code-status quotes are secondary; the file is the artifact. Display-only; no `cam/core/`, prompt, model, or pipeline change. OPEN (Tzvi's call, not bundled): should the strip conditional-fire only when the indigo box's reasoning is thin, rather than always fire? n=1 run, DIRECTIONAL, NOT promoted. Part 2 (naming the SPECIFIC deal dependency) stays DEFERRED — needs a pipeline change.

> **2026-07-05 UPDATE-2 (provenance work, after the strip work above).** Step 399 Priority Exposure Readiness Census written + committed (`git add -f`, msg "399 priority exposure readiness census"): `build_log/399_priority_exposure_readiness_census.md`. Key census findings (n=1, Atlas frozen run, DIRECTIONAL, NOT promoted): only **7/32** coverage items carry assessed `use_impact`; **30/32** exposure statements are `exposure_source=schema` (default template, not lease-specific); all provenance fields already reach `buildItem(a)` and were hidden. Diagnosis: provenance + coverage problem, NOT ontology. Doctrine intact (Risk stays action-type; no Option-B severity-redefine). Step 400 (`282abb9` + SHA patch `4f6e2c5`, local/unpushed) SHIPPED display-only provenance chips: exposure chip (Lease-specific / Default / unknown) + materiality provenance row (purple "Assessed materiality" on the 7, grey "Default materiality" on the 25). Muted wording by design (no "Unassessed"/"Fallback" sirens). **Badge rename BLOCKED and flagged:** "PRIORITY REVIEW" chip (line ~18276) is MIXED-driver — coverage LPs fire on `hard_flag`/verdict_distance, synthesis cards fire on `severity==="HIGH"`, sharing one HTML chip. Renaming to "Verdict Spread" would mislabel synthesis cards. Type-conditional rename needs its own authorized step. Sorting DEFERRED (Tzvi's call, not forgotten). OPEN pipeline question (needs 2nd run first): why does `use_impact` attach to only a minority of cards? NOT investigated — gated behind confirming sparsity on the live 2026-07-05 run (`1771a4`). Priority Exposure / Top Client Traps surface NOT built — deferred until sparsity confirmed + coverage broadened.

> **2026-07-05 UPDATE-3 (sidebar + census 2nd run).** (a) Step 402 (`f52072e`, local/unpushed) SHIPPED two sidebar display fixes that were LOST to a numbering collision (Chat reused the "400" slot for provenance work, overwriting an earlier sidebar handoff, so those changes were never built until re-issued as 402): RISK section now opens by default to its three sub-headers (COVERAGE GAPS / CROSS-CLAUSE RISKS / ONE-SIDED TERMS) all collapsed; NEEDS REVIEW / IMPROVEMENT / ADDRESSED start collapsed; issue-card titles now wrap to 2 lines instead of ellipsis-clipping. Implemented via the existing `_NAV_SUBGROUP_DEFAULT_COLLAPSED` pattern + new `_NAV_SECTION_DEFAULT_COLLAPSED` dict; defaults apply only when localStorage has no saved toggle. (b) CENSUS 2ND RUN: re-ran the 399 census on a fresh same-lease Atlas run (`lease_review_20260705_232259_443e33`). Assessed `use_impact` = **9/32** (was 7/32 on 2026-06-11) — still a minority; sparsity REPRODUCED across two runs, no longer n=1. Exposure-template wall held identically (30 schema-default). Assessed set = June's 7 + LP-02 + LP-28; LP-05 flipped again (context_dependent/1-1-1 → harmful/2-1 — now 3 different classifications across 3 runs of the same lease). Badge (hard_flag) fired on 5 cards, ALL 3-0 unanimous agreement — confirms across both runs that the coverage badge is verdict_distance-driven, NOT outcome-disagreement-driven (strengthens the 401 badge-split case). Sparsity investigation gate now OPEN (why does use_impact attach to only a minority?) — pipeline dive, not yet done. Still DIRECTIONAL / NOT promoted (one lease, two runs). 399b two-run comparison note offered but not yet written.

> **2026-07-05 UPDATE-4 (classifier boundary trace — the key architecture finding).** Step 399c (`f663f9d`, trace-only, no code change) traced why LP-16 Parking displays in NEEDS REVIEW despite assessed `harmful`/medium consequence. **THE FINDING: the `partial`-coverage routing is BLIND to `use_consequence` direction.** LP-16 path: `partial_typical` + `uiMat=medium` → `matTier=MEDIUM` → `isHighSev=false` → `sevTriage()` → `improvement`, then `hard_flag=true` promotes to `review_needed`. The `harmful` value is NEVER READ in partial routing — severity derives only from `use_impact.materiality` and `partial_class`. Proof by twin: **LP-10 has the same fields (partial/medium/harmful) but no hard_flag → lands in IMPROVEMENT** ("protection exists, could be tightened"), i.e. a harmful-consequence card labeled a drafting nicety. LP-16 sits one bucket higher (NEEDS REVIEW) ONLY because hard_flag floored it up — and hard_flag fires on verdict_distance, not consequence (per 399b). So Parking is in NEEDS REVIEW essentially by accident of a verdict-spread flag; its harmful twin without the flag is in IMPROVEMENT. **Architecture conclusion (confirmed, not speculative): RISK is confidence/materiality-first; `harmful` consequence is orthogonal to the action buckets; a consequence-led Priority Exposure surface MUST sit ABOVE the buckets — it cannot be a RISK redefinition without conflating axes.** Not a classifier bug (LP-16 follows the general partial-card rule, not a specific demotion). Recorded as EVIDENCE for the parked Priority Exposure decision; NO build. Future option (product judgment, named, NOT taken): route `mat=medium && gap=harmful` to HIGH in `sevTriage()` — one-line change, deliberate axis-mixing, record before ever doing. DIRECTIONAL, one lease. The Current-state (2026-07-03) block below is otherwise accurate; the 398 line there reads "handed off" and is now "shipped + reworded-handoff-pending."

> **CANONICAL LOCATION: this file (`Docs/NEW_THREAD_PROMPT.md`) is the single source of truth.** A second copy at `build_log/NEW_THREAD_PROMPT.md` exists for historical reasons and may be stale — ignore it; update only this one. (As of 2026-06-13 both hold the same content; going forward, refresh THIS file and treat the build_log copy as a stub.)

> **First task of any new thread: confirm this file is current.** If the most recent dated work below predates your actual last session, this is stale — check `Docs/CAM_Current_State.md` and `Docs/Patent_Current_State.md` frontiers, which are the authoritative end-of-session state, and refresh this file.

---

## What this project is

CAM (Constrained Assertion Method) is a framework for governing **when** an AI system may assert a conclusion. The patent is the endgame (licensing via broker; non-provisional target ~Nov 2026; attorney conversation before Sept 2026). The vered.ai lease analyzer is the first validation instance, **not the invention**. Tzvi is the sole inventor.

**Core doctrine that must not soften:** the action-type ontology (Risk / Improvement / Review Needed / Addressed are *action-type categories, not confidence levels and not leverage*); minority never silenced; measure-before-enforce; n=1 findings stay DIRECTIONAL and out of the patent record except as example. Full guardrail list (now #1–#18) in `Patent_Current_State.md`.

---

## How this session works (read before doing anything)

- **Two filesystems that cannot see each other.** The **filesystem MCP** (this Desktop session) reaches ONLY OneDrive paths (`C:\Users\Owner\OneDrive\CAM\...`). The **bash container** reaches ONLY `/mnt/...` and `/home/claude`. They are isolated. The MCP cannot run git or read container paths; the container cannot read OneDrive. For docx, build in the container from content, then deliver to `/mnt/user-data/outputs/`.
- **str_replace fails on OneDrive paths** — use `write_file` (full overwrite) or `edit_file` (line edits). `write_file` OVERWRITES the whole file — use `edit_file` to append. (A supplement was once destroyed by a careless `write_file`; be careful.)
- **Chat (this instance) writes paper; Claude Code implements + commits.** Chat does not run the pipeline or push. Sessions end with a copy-paste commit prompt for Claude Code.
- **`git add .` is PROHIBITED from the repo root** — the OneDrive `.tmp.driveupload` staging folder (heavily populated, known hazard) would get swept in. Always stage explicit paths. **BOTH `build_log/` AND `Docs/` are gitignored** — Code must force-add files in either with `-f` (confirmed empirically 2026-06-14 when committing `Docs/` edits as `7ee7919`: without `-f` the add silently no-ops and the commit misses the changes). Earlier versions of this doc wrongly said `Docs/` was not gitignored; it is.
- **Commit local, do NOT push, unless told.**
- **Stack-freeze is active.** Three evaluator models are held constant (A=claude-sonnet-4-6, B=gpt-5.4/5.5, C=grok-4.3) so output changes attribute to code, not model swaps. Do NOT swap models mid-investigation. This is a controlled variable, not a parked setting.
- Style: terse, no em-dashes, markdown in docs. Tzvi dislikes unprompted "want me to stop here?" offers, but DOES want honest pushback when continuing would be low-value.

---

## Current state (2026-07-03)

- **Phase 1 external CRE review — COMPLETE (interim), n=3, PROVISIONAL, NOT promoted to patent record.** Three experienced CRE attorneys answered the blind packet: **R1 Yaima S.** (blind), **R2 Max L.** (blind; REIT in-house/ex-Dentons), **R3 Josh** (cold read on the packet, project-aware — near-blind tier, NOT a stranger; do not average as equivalent to blind). Canonical record: `build_log/lawyer_packet_COMBINED_RESULTS.md`. R3 detail + capstone: `build_log/R3 josh and corrections.md`. Interim synthesis: `build_log/Phase1_External_Review_Interim.md`.
  - **Clear calls converge** (tool + all three): BOKF Risk, late-delivery Risk, materiality, §34-vs-§3.1(C) credit, min fix.
  - **Disagreement concentrates on the already-unstable clauses** (LP-19, LP-26), NOT at random — tool wobble tracks genuine lawyer wobble.
  - **LP-19:** leans 2-1 toward genuine contestability; the two "depends" votes (R1, R3) CLUSTER ON THE SAME REASON (tenant-operational context). Cleanest near-resolution. A clean blind R4 would settle it.
  - **LP-26:** three-way bucket split (Risk/Standard/Improvement); multidimensional. New falsifiable claim (Josh): §19.2 SNDA structure may be ATYPICAL ("backwards"). "Unusual drafting + confident" is real → unusual ≠ ambiguous.
  - **Strongest qualitative finding (Josh capstone, verbatim on disk):** flag-worthiness often depends on the BUSINESS DEAL, which is not in the document — effectively **"C is not derivable from A alone."** Third independent route to context-dependency (R1 asset-class, R2 leverage, R3 business-deal). Reframes single-bucket as, on a SUBSET of clauses, asking an unanswerable question (design problem, not just lossiness). **A→C SOMETIMES fails, not always.**
  - **Open frontier: NO DETECTOR** for which clauses are context-dependent — the real next measurement question. Possible (distrust-because-attractive) link to H-BORDER-01, parked.
  - **Named methodology failure mode (caught live):** reason-without-provenance — a reason detached from {reviewer, clause, question} can manufacture a false finding.
  - **Hypotheses stay in the holding pen** (H-STD-01 / H-IMP-01 / H-BORDER-01, `build_log/H-STD-01_candidate.md`); NONE promoted.
- **Context-dependency VISIBILITY build — Part 1 HANDED OFF, not yet built.** Reading real source this session established the "missing bucket" the reviewers pointed at is **largely ALREADY COMPUTED**: Stage 5e (`cam/adapters/lease_review/lease_use_impact.py`) produces `use_impact` per coverage item (`use_consequence` incl. `context_dependent`, `use_reasoning`, `materiality`, `evaluator_agreement`), it IS persisted to `pipeline_results.json`, and IS served to the client intact via `get_job_results` in `app/main.py` — but rendered NOWHERE user-facing (not synopsis PDF, not chat context; on-screen `app.js` TBC). Gap is VISIBILITY, not computation. Handoff: `build_log/398_chat_instruction.md`. Guardrails: no `cam/core/`, no new model call/prompt, no hardcoded content, don't call it a "bucket" ("Context Dependency"/"What this depends on"), strictness=display-only, render only when real data, STOP-and-report if `use_impact` not reaching `app.js`. **Part 2 (Josh's deal-dependency, distinct from use-profile) DEFERRED.**
- **`CAM_Current_State.md` has a 2026-07-03 pointer block; `External_Validation_Findings_2026_05_10.md` has a pointer to the panel** (that file is the May single-run only, NOT the panel — do not conflate its LP-11 "backwards" with Josh's LP-26 §19.2 "backwards").
- **Reviewer phase is NO LONGER GATING.** Marginal value of reviewer #4 < value of advancing. A clean blind R4 is welcome if it falls in your lap (would settle LP-19); do NOT restart the Upwork recruitment grind.
- **Patent record unchanged this session** — nothing reduced-to-practice; `Patent_Current_State.md` correctly untouched. Everything above is provisional and stays out of the patent record except as example.

---

## Prior state (2026-06-14)

- **Patent record:** through **Supplement #25** (Architecture A Phase 2). `Patent_Current_State.md` is current through 2026-06-13 and is the orientation doc — read it first for patent state. The EDGAR corpus is NOT a patent contribution and NOT a validation result — it is a fixture asset only; do not add it to the patent record.
- **External corpus (NEW 2026-06-14):** first external real-lease fixture set built from SEC filings — 8 executed commercial leases (10-K/10-Q Exhibit 10.x), 4 property types (office / lab-office / retail-NNN / industrial), 6 jurisdictions (CA/CO/MA/NC/OK/SC), populated-vs-absent work-scope split 3/5. 48/48 fixture-integrity checks pass. Fixtures in `05 Lease Analyzer/test_data/tenants/`; manifest `05 Lease Analyzer/test_data/edgar_corpus_manifest.json`; build script `build_log/build_edgar_corpus.py` (regenerable). NO pipeline run yet — corpus is built, not evaluated. Sharpest fixtures: the **Atreca pair** (same tenant/landlord-family/date, EX-10.18 existing-building Landlord's-Work vs EX-10.19 to-be-constructed shell + tenant-built TIs = a near-controlled contrast in the STRUCTURE of landlord work, both populated) and **BOKF As-Is Work Letter** (exhibit present but obligation functionally absent = the document-presence-vs-obligation-substance edge case). NOTE: the cleanest populated-vs-ABSENT contrast is a populated lease (e.g. Atreca EX-10.18) vs an absent one (DiVall Wendy's NNN, or Quanterix/SolidPower tenant-work), NOT the two Atreca leases against each other — both Atreca leases are populated.
- **Build:** stack-frozen, in DEF-010 recall-stability DIAGNOSIS (not measurement). Last shipped code was the 378/383 governance + DEF-010a work; nothing shipped since. Corpus-building is a test_data asset, not a build change — freeze intact.
- **Most recent commits (local, UNPUSHED):** `b1ec8e7` (EDGAR mini-corpus: 8 fixtures + manifest + build script, 2026-06-14). Prior: `0c591cd` (Step 396 + Packet 02), `a5905d1` (394c/395/395b + Packet 01 + patent docs). The 2026-06-13 paper work was committed before this session.
- **Schema:** v2.2.0, 32 LPs + 212 elements.

---

## What the last four sessions produced (paper arc, all freeze-safe)

| Artifact | What it is |
|---|---|
| **Supplement #24** (2026-06-12) | Cross-domain auditor lineage (FEVER/GPQA/SciFact/ContractNLI); three-layer directional governance (axis discipline / trace auditor / materiality-routing); refined minority doctrine. Guardrails #17, #18. |
| **§8.3 absence census** (393/394a/394c) | The Landlord's-Work/fixed-commencement trap is a trap of ABSENCE the four present-text axes miss. Atlas = one clean instance; Albireo = clean counter-instance; "zero cleanly-measurable cross-lease RECURRENCE instances" (strict wording). (b) Axis-2-variant vs (c) fifth-axis stays OPEN. |
| **Step 395 / 395b** | Leverage / Client Advantage as an orthogonal SECOND axis (protective disposition + advantage disposition). Favorable-absence census: AXIS WARRANTED (present-text leverage recurs; favorable absence real but drafting-dependent). NOT a patent contribution yet — design result. |
| **Step 396** | DEF-010 variance classification. LP-03 = generation defect, majority=SIGNAL, safe to gate. LP-19/LP-26 = consequence-nondeterminism, majority=UNKNOWN, LAWYER-GATED. 11-finding tail untraced. Completeness gate must be SPLIT by class. |
| **Packet 01 / Packet 02 (BLIND)** | Blind CRE-lawyer validation packets. Packet 02 supersedes 01 for sending (§8.3 Atlas/Albireo + LP-19 + LP-26). The B2/C2 "confident vs genuinely-depends" question resolves the Step 396 class-3-vs-class-4 fork. docx in `build_log/`. |
| **Supplement #25** (2026-06-13) | Architecture A Phase 2: LP-layer verdict distance + Stage 5f confidence cap (formalizes Steps 351/351b/352 RTP). Second instantiation of Supplement #18 ordinal-distance governance. Deliberate-gap rank scale; NOT_ASSESSED sentinel. |
| **Step 397** (2026-06-13) | Closed-form question-set design (paper half). Designed **Axis 5** (obligation-against-foreseeable-failure / absence-of-paired-relief) to give the §8.3 trap a closed-form home. On-paper forcing-test: Axis 5 fires on LP-03 §8.3, stays silent on LP-26/§11.2 (the discrimination property). World-question stays lawyer-gated. |
| **Attorney Question Bundle** (2026-06-13) | `Docs/Attorney_Question_Bundle.md`. Consolidated, theme-grouped, grounded question list for the patent-attorney conversation. Updates the June-9 review's Part 4 list + four parked items + today's new questions. |

---

## THE BINDING CONSTRAINT (read this before picking next work)

**UPDATED 2026-07-03.** The `CAM_Project_Review_2026_06_09.md` named the project's largest risk as **external validation at zero**. That constraint has now MOVED TWICE. First (2026-06-14) the EDGAR corpus closed the "no real leases" gap. Then (2026-07-03) the Phase 1 blind panel closed the "no human reaction" gap: three CRE attorneys reacted to real findings. **External validation is no longer zero.** It is n=3, provisional, interim — meaningfully more than nothing, meaningfully less than validated.

**The binding constraint is now TWO things, both downstream of "the tool broadly tracks specialists":**
1. **No willingness-to-pay / usefulness signal.** No one has indicated they would USE or PAY for any version of the output. The panel showed the tool is roughly right; it did not show anyone wants it. This is the new top uncertainty.
2. **No detector for context-dependency.** The panel's strongest finding is that flag-worthiness depends, on a SUBSET of clauses, on business context not in the document ("C not derivable from A alone"). There is no way yet to PREDICT which clauses those are. This is the real next MEASUREMENT question.

**The discipline for the next session: do NOT mistake motion for progress, and do NOT act on the panel as if it were a finding.** The panel is n=3 and provisional. The temptations now are (a) rip out single-bucket and build A/B/C — DON'T, it's a hypothesis (H-STD-01), not a mandate; (b) build a leverage/aggressiveness SETTING that alters detection — DON'T, that is consent-wrapped over-withholding (prohibited); (c) recruit more reviewers — no longer gating, don't restart the grind; (d) expand to new domains/modules — premature, instance one is not yet monetized. The moves that actually advance the trajectory are below.

---

## Next-session menu (in honest priority order)

**Tier 1 — moves the (new) binding constraint:**
- **Run the 398 UI-visibility build** (`build_log/398_chat_instruction.md`) via Claude Code — surface the already-computed context-dependency (`use_impact`) in the on-screen finding cards. Display-layer only, guardrailed. This is the prerequisite to having something showable. When Code finishes it writes `build_log/398_code_status.md`; only then is Part 1 shipped (move the CAM_Current_State line from "handed off" to "shipped").
- **Willingness-to-pay / usefulness signal — run OPPORTUNISTICALLY, not via a recruited panel.** Narrowed hypothesis (Josh handed you this): *does NAMING THE MISSING CONTEXT make the output more useful?* Compare bare bucket ("Risk") vs structural-read + named-dependency + the question-to-answer. Show it 1-on-1 to whoever is reachable (a lawyer contact, a business contact); read the "why" as the primary signal; capture respondent type (lawyer vs tenant) for segmentation. Mockup preference ≠ actual payment — treat a win as "worth pursuing," not "validated demand." Caution: length confound (Version B is longer); the closing question is the real variable.
- **Attorney prep (method / evidence / roadmap structure)** — HAS THE CALENDAR: patent-attorney conversation before September, non-provisional ~November. Does NOT need more reviewers. Instrument exists: `Docs/Attorney_Question_Bundle.md`. Discipline: the patentable object is the METHOD (built, reduced-to-practice); the panel is EVIDENCE-IT-WORKS (honestly bounded, n=3); the hypotheses are ROADMAP. Do NOT import scientific-completeness caveats ("only n=3, nothing proven") into a PATENTABILITY conversation — that under-sells the invention. Lead with the method, use validation as support, present hypotheses as continuation.

**Tier 2 — heavy patent keystones (each its own fresh session, not a tail-end block):**
- **Prior-art triage memo** (Gap 3 / Bundle B1) — structured search of the four closest families: selective prediction/learning-to-defer, conformal prediction, self-consistency/ensemble, LLM-as-judge/debate. One memo: closest art + distinguishing feature per contribution. Prerequisite to the next item.
- **Claim-priority memo** (Gap 3) — rank the 25 supplements by novelty × support × detectability. The keystone the attorney conversation turns on. Do AFTER prior-art.

**Tier 3 — lighter owed odds-and-ends (an afternoon each):**
- Data-handling one-page statement + access-code upgrade (Gap 8).
- Cost/runtime capture (Gap 9) — one probe script; licensing math needs it.
- Tamper-evident evidence repo (Gap 4) — private git remote with the ignore lifted for Docs/build_log; hashed dated commits for the conception/RTP narrative.
- `twilio_2FA_recovery_code.txt` sits in `Docs/` in plaintext on a synced drive — move to a password manager.

**Gated / parked (NOT actionable now — do not start):**
- **Part 2 of the context-dependency build** (Josh's deal-dependency, distinct from the use-profile Stage 5e conditions on) — needs a pipeline/prompt change; do NOT bundle with Part 1.
- **A/B/C output restructure** (H-STD-01) — hypothesis, not a build order. Test via the WTP signal first.
- **Any detection-altering strictness/leverage setting** — PROHIBITED (consent-wrapped over-withholding).
- DESIGNED recall-stability run on the EDGAR corpus — legitimate but must be hypothesis-first and freeze-respecting. Not exploratory. Do NOT swap models.
- LP-03 generation gate (the one safe DEF-010 code fix) — touches the freeze; deliberate decision.
- Axis-5 build — needs the lawyer panel's world-question answer first.
- New domains/modules — premature; instance one not monetized.
- DEF-002 — blocked by recall instability.

---

## Two different lawyer conversations — do not conflate

- **CRE attorney PANEL** — answers "is this lease finding CORRECT?" (the world-question). Phase 1 COMPLETE (interim, n=3: R1 Yaima / R2 Max / R3 Josh); records in `build_log/lawyer_packet_COMBINED_RESULTS.md`. No longer gating; a clean blind R4 would settle the LP-19 confidence split but is not required. (The blind instrument used was the Section 1–4 packet; the earlier `Packet 02` docx in `build_log/` was the draft of it.)
- **PATENT attorney** — answers "what claim scope?" Instrument: `Docs/Attorney_Question_Bundle.md` (built). Different person, different conversation, and the one with the CALENDAR (before September). NOT yet had.

---

## Patent documentation protocol (standing)

New patent insight → in the SAME session: (1) write `Docs/Patent_Supplement_YYYY_MM_DD*.md` (full record); (2) update `Docs/Patent_Current_State.md` (Contribution Map + Supplement Index + Patent Sentences + Guardrails + Canonical Examples as applicable). The two files are always updated together. Strict wording on absence findings: "zero cleanly-measurable cross-lease RECURRENCE instances," NEVER "zero instances exist." Quantitative lease findings stay DIRECTIONAL / out of the patent record except as example.

---

## LP-ID dual-numbering hazard (read before citing any "LP-NN")

The directional prototype (Steps 389–397) uses LP IDs that DO NOT all match `cam/adapters/lease_review/lease_provision_taxonomy.py`. Partial collision (the dangerous kind): prototype LP-03/LP-26 coincide with taxonomy; prototype LP-11/LP-15/LP-19 do NOT. Crosswalk: `Docs/LP_ID_Crosswalk_Directional_Prototype_to_Taxonomy.md`. Directional-method docs use prototype IDs; patent/attorney-facing summaries must cite BOTH.

---

## Key paths

- **Orientation (read first):** `Docs/Patent_Current_State.md` (patent), `Docs/CAM_Current_State.md` (build)
- **Strategic ledger:** `Docs/CAM_Project_Review_2026_06_09.md` (gaps + 12-week sequencing), `build_log/parked_strategic_ideas.md`
- **Attorney prep:** `Docs/Attorney_Question_Bundle.md`
- **Defects:** `build_log/defects.md` (DEF-002 blocked, DEF-010 sorted by Step 396)
- **Closed-form module (RTP):** `cam/adapters/lease_review/lease_closed_form_directional.py`; results in `389`/`391`/`397` build_log files
- **Verdict distance (Supp #25):** `cam/adapters/lease_review/lease_verdict_distance.py`
- **Fixtures:** `05 Lease Analyzer/test_data/tenants/` (atlas_meridian_warehouse_lease.txt, albireo_10postoffice_lease.txt; + EDGAR corpus 2026-06-14: bokf/everbridge/ncino/atreca x2/quanterix/divall/solidpower). Manifest: `05 Lease Analyzer/test_data/edgar_corpus_manifest.json`. Build script: `build_log/build_edgar_corpus.py`.
- **API keys:** `C:\Users\Owner\OneDrive\CAM\DoubleCheck\doublecheck-api\api_keys\.env`
- **Deployment:** Railway ("grand-nature"), GitHub `zvido1/cam`, auto-deploys on push to main. Browser: **Edge** (not Chrome).

---

## Housekeeping flagged this session

- **OneDrive `.tmp.driveupload` is heavily populated** (CAM root) — OneDrive's own sync staging is backed up; NOT a Google Drive conflict (no Google Drive markers found in work folders). Safe to leave (gitignored) but worth clearing when OneDrive is paused if sync stays stuck. This is the reason `git add .` is prohibited.
- **Two NEW_THREAD_PROMPT.md copies existed** (Docs/ and build_log/) and had drifted to different content — resolved 2026-06-13 by making `Docs/` canonical. Keep it that way; do not let a second copy go stale.
- **`Docs/` is gitignored (corrected 2026-06-14).** This doc previously claimed `Docs/` was NOT gitignored; that was wrong and is now fixed in the session-rules block above. Consequence worth noting: the canonical orientation docs (this file, `CAM_Current_State.md`, `Patent_Current_State.md`) all live in a gitignored directory, so they are version-controlled ONLY via explicit `-f` force-adds. A future move to a tamper-evident evidence repo (Tier-3 item) should lift the ignore on `Docs/` so the patent-narrative docs get clean hashed history without force-adds.
- **Refresh this file at the end of any substantive session** so the next thread orients correctly. It was ~4 weeks stale (build_log copy) / ~1 day-arc stale (Docs copy) before the 2026-06-13 overwrite.
