# Step 538 — Instruction

**Received:** 2026-09-03, verbatim from Tzvi in conversation, transcribed to disk before
execution per CLAUDE.md Reporting Integrity Rule 7.

---

Step 538. LP-20 is displayed as covered with 0 of 7 elements present.
Fix the categorisation, not the verdict.

Step 537: LP-20 returned review_needed, requires_attention True, 0 of 7
elements present — and appears inside the 27 "covered" count. A retail
tenant with no exclusivity protection is told the provision is covered.

Step 521 recorded this on Atlas as an arithmetic inconsistency. On a
shopping-centre lease it materially misleads.

1. What determines the "covered" bucket? Quote it. Is review_needed in it
   deliberately, or by omission from a membership test?
2. Report every coverage_state and where each lands, across all nine
   surfaces from Step 477's census — screen, both annotators, summary,
   batch, API.
3. Propose the fix. A state with zero elements present must not read as
   covered, on any surface. Do NOT change the panel's verdict — the
   verdict is correct and hard-won.
4. Verify by artefact: generate DOCX and PDF from the Step-537 result and
   quote what a reader sees for LP-20 before and after.

Do NOT touch the qualifier pass, the locator, or the seam fallback. Those
are three separate findings and each deserves its own step.
