CLOSEOUT BLOCKS — 404-407 Stage 5e / Priority Exposure eligibility arc.
Dated 2026-07-07. NEWEST-ON-TOP. Code: insert each block at the marked anchor, do not alter existing blocks.

================================================================================
BLOCK 1 — for Docs/CAM_Current_State.md
INSERT: immediately ABOVE the line that begins
  "> **=== LATEST STATUS 2026-07-05 (read THIS before the 2026-07-03 block above/below..."
so this new block becomes the newest frontier at the top of the nested-> stack.
================================================================================

> **=== LATEST STATUS 2026-07-07 (read THIS FIRST — newest frontier. Stage 5e sparsity RESOLVED as traceable + fixable; Priority Exposure re-gated onto compound-consequence architecture. Full arc: build_log/404–407. ===**
>
> **One-line conclusion:** LP-level widened Stage 5e WORKS and TRANSFERS (Atlas + Atreca, 4 runs), but Priority Exposure stays BLOCKED because compound/CRX findings need FINDING-LEVEL consequence assessment that LP-widening structurally cannot provide. Widened gate ships behind a DEFAULT-OFF flag; main behaviorally unchanged. All DIRECTIONAL, two leases, NOT promoted, NOT patent record.
>
> **404 — sparsity traced (read-only, code-only; SHA `a262c92`).** `use_impact` sparsity is a deterministic eligibility filter, NOT cap/prune/null/random-selection. Gate = `_should_assess()` in `cam/adapters/lease_review/lease_use_impact.py` (an ADAPTER, not `cam/core/` — NOT patent-frozen; its `_merge_verdicts` governance rules ARE a core-semantics instance and stay untouched). Gate admits `missing` + `review_needed` unconditionally, and `partial` ONLY when missing-element fraction ≥50%. Most cards land light-partial (<50% missing) and are silently excluded — a COVERAGE-completeness gate serving a CONSEQUENCE-coverage goal. Run-to-run assessed-set movement (7↔9 of 32) = UPSTREAM coverage-state / element-verdict wobble crossing the deterministic gate, not selection nondeterminism. Distinct from INSIDE-5e value churn (LP-05).
>
> **405 — prior COV record reconciled + widening design (SHA `9341b9a`).** The apparent 375J-vs-375M tension resolves by LAYER: 375J's "widen the 8/32 gate" and 375M's "COV is finding-level provenance, NOT LP-widening" were about the DIRECTIONAL layer. COV-A (`771f1ef`/`fc8d3dc`/`8de0d74`, on main) shipped finding-level consequence for directional findings via a SEPARATE module `lease_finding_consequence.py` — it NEVER touched coverage-LP `_should_assess`. So the coverage-LP 50% gate was DEFERRED, not decided against; 404 confirmed it live. LP-level vs finding-level consequence is a REAL architecture distinction, not a settled ruling. 405 authorized Option A (all-partial widening) as CONTROLLED DIAGNOSTIC only, token/chunking-gated, N≥2, behind a default-off flag. Multi-finding-per-LP caveat kept LIVE (silent-failure mode: one LP with two opposite-consequence findings → per-LP use_impact misleads with no error).
>
> **406 — Atlas widened diagnostic (SHA `8b9ed5d`).** Widened `_should_assess` behind `widen_partial` flag (default-off; narrow ≥50% path preserved as baseline) + chunking (~11 LPs/chunk, merged into existing `_merge_verdicts` unchanged). N=2 on two frozen Atlas artifacts. Result: wide-eligible 27/27 BOTH runs (ZERO eligibility churn — the LP-02/LP-28 narrow churn absorbed); newly-admitted yield 18/20 decisive (90%), 0 abstain — signal, not noise; value churn 3/27 concentrated on genuinely ambiguous clauses (LP-05 pre-existing, LP-17/LP-21 borderline). No truncation/fallback. Validated widening as a DIAGNOSTIC on Atlas — NOT final architecture.
>
> **407 — Atreca transfer test (SHA `46fefaf`; 406-report cleanup `ee5eb3a`; harness `build_log/run_407_gate2.py`).** FIRST real EDGAR pipeline run (full Mode C ~23 min × N=2). Fixture: Atreca EX-10.18, 450 East Jamie Ct, South SF — lab/office, CROSS-TYPE vs Atlas warehouse/industrial. Transfer HELD: wide-eligible 19/19 both runs (0 eligibility churn), value churn 0/19 (cleaner than Atlas), newly-admitted yield 8/8 decisive (100%), no truncation/fallback. **THE 407 FINDING (the reason not to promote): Atreca surfaced 6 Stage-7 COMPOUND findings (CRX-01..06) spanning LP-01/LP-11/LP-27 etc., ALL marked `not_assessed` for consequence.** A compound risk ("one-sided enforcement machinery" = LP-01+LP-11+LP-27 interacting) is a distinct legal pattern that per-LP `use_impact` CANNOT represent, no matter how wide the LP gate opens. This proves LP-widening ≠ sufficient for Priority Exposure. (NOTE: this is NOT the original 405 multi-finding-per-LP coverage-card caveat — that asked whether one LP yields two COVERAGE CARDS; still 1:1 on both leases, still UNTESTED. The compound-finding gap is a related but distinct, now-OBSERVED gap.)
>
> **FINAL DECISION (this arc):** (1) Do NOT flip widened LP-level Stage 5e on by default yet. (2) Do NOT build Priority Exposure yet. (3) LP-level widening = VALIDATED diagnostic/prototype evidence for COVERAGE cards (transfers across two lease types). (4) Finding-level consequence for compound/CRX findings = the NEXT architecture question. (5) **The open design question for next session: can COV-A's finding-level consequence architecture EXTEND to compound/CRX findings, or do compound findings need their OWN consequence lane?** — spec this FRESH next session (408), do NOT draft it tired.
>
> **STANDING CAVEATS:** Atlas+Atreca = two leases, both CA-governed tenant-perspective — NOT full validation; other jurisdictions/landlord-perspective/NNN untested. Original multi-finding-per-LP coverage-card caveat remains UNTESTED (two clean 1:1 leases = absence of evidence, not resolution). LP-widening helps coverage cards but cannot represent cross-provision traps. Any future Priority Exposure surface must rank COVERAGE + DIRECTIONAL + COMPOUND findings with honest consequence provenance. Harness note for future diagnostics: `run_lease_coverage_only` stores `use_impact_governance`, not `use_impact_meta` — fix the field name to capture per-call token usage directly rather than from console logs.

================================================================================
BLOCK 2 — for Docs/NEW_THREAD_PROMPT.md
INSERT: immediately ABOVE the line that begins
  "> **2026-07-05 UPDATE (read before the Current-state block below).**"
so it becomes the newest UPDATE block at the top of the stack.
ALSO update the "_Last updated:_" line at the very top of the file to read (replace the existing italic line):
  _Last updated: 2026-07-07 (404–407 Stage 5e / Priority Exposure eligibility arc CLOSED — LP-widening works + transfers (Atlas+Atreca, default-off flag), but Priority Exposure re-gated onto compound/CRX finding-level consequence. Next: 408 compound-consequence architecture, fresh. Prior: 2026-07-05 UI provenance + 399c classifier finding.)_
================================================================================

> **2026-07-07 UPDATE (read FIRST — newest frontier; closes the 404–407 arc).** The Stage 5e sparsity investigation is RESOLVED and the Priority Exposure blocker is RE-DIAGNOSED. Chain: **404** (`a262c92`) traced `use_impact` sparsity to the deterministic `_should_assess()` gate in `cam/adapters/lease_review/lease_use_impact.py` (adapter, not frozen core) — admits missing/review_needed always, partial only at ≥50% missing elements; run-to-run set movement = upstream coverage-state wobble crossing the gate, not random selection. **405** (`9341b9a`) reconciled the prior COV record: COV-A shipped finding-level consequence for DIRECTIONAL findings via `lease_finding_consequence.py` and never touched coverage-LP `_should_assess`, so the 50% gate was deferred not decided; authorized Option A (all-partial widening) as controlled diagnostic behind a default-off flag. **406** (`8b9ed5d`) Atlas widened diagnostic, N=2 on frozen artifacts: 27/27 wide-eligible both runs (0 eligibility churn), newly-admitted yield 18/20 decisive (90%), value churn 3/27 on ambiguous clauses, chunking clean. **407** (`46fefaf`; 406 cleanup `ee5eb3a`; harness `run_407_gate2.py`) FIRST EDGAR pipeline run — Atreca EX-10.18 lab/office (cross-type vs Atlas), full Mode C ~23min × 2: transfer HELD (19/19 wide both runs, 0 eligibility churn, 0 value churn, newly-admitted 8/8 decisive 100%, no truncation/fallback). **KEY 407 FINDING:** Atreca surfaced 6 compound CRX findings (LP-01/LP-11/LP-27 interacting), ALL `not_assessed` for consequence — per-LP `use_impact` structurally cannot represent a cross-provision trap, proving LP-widening is NOT sufficient for Priority Exposure. **DECISION:** widened gate stays DEFAULT-OFF; Priority Exposure NOT built; LP-widening = validated diagnostic for coverage cards; compound/CRX finding-level consequence = the next architecture question. **NEXT SESSION STARTS HERE (408, spec fresh — do NOT reuse a tired draft): can COV-A's finding-level consequence architecture extend to compound/CRX findings, or do they need their own consequence lane?** Standing caveats: two CA tenant-side leases ≠ validation; original multi-finding-per-LP coverage-card caveat still UNTESTED (both leases 1:1); all DIRECTIONAL, NOT promoted, NOT patent record. Doctrine intact: Risk stays action-type; consequence orthogonal to buckets; Priority Exposure sits ABOVE buckets, never a Risk redefinition.
