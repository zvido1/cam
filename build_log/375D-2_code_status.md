# Step 375D-2 — Status: Role-B context-sensitivity harnesses (WRITTEN, NOT RUN)
**Date:** 2026-06-03  **Mode:** Code wrote two harnesses; **Code did NOT run them** (no provider keys here,
and runs cost money). **NO production change** — both scripts live in `build_log/`, call the REAL code path,
write only `build_log/375D2_*.json`. No prompt/severity/routing/classifier change.

## Corrected conclusion (state as HYPOTHESIS, not verdict — per the brief)
**Proven (375D):** directional HIGH/Risk swings **21 → 12 → 7 → 15 → 13** on byte-identical frozen input,
integrity clean all 5 passes; directional candidate count **26/26/26/20/26** (pass 4 lost
**LP-03/06/11/17/18/24**); **B is the swing role** in the full pass; and in **isolation** the **sampled 9**
candidates were near-deterministic (89/90 `mismatch_confirmed`, 1 `unclear`, 0 integrity fails).

**NOT yet proven (what 375D-2 settles):**
- **(a)** that the cause is specifically **batching** — vs candidate **order**, **neighbor set**, a
  **prompt-format difference** between the full and isolated harness, **upstream candidate-set churn**, or a
  **mix**. Context-sensitivity is the **leading hypothesis, not the verdict.**
- **(b)** that **all** flippers are isolation-stable — the 375D roleB run sampled only **5 of 14** flippers
  (LP-01/02/03/04/07) and **4 of 12** unanimous controls; the other 9 flippers are untested.

> **Working statement:** *Role B is highly variable inside full Stage-7 execution but nearly deterministic on
> the sampled candidates in isolation. The leading hypothesis is batch/prompt/context sensitivity; 375D-2
> determines whether the driver is batch size, ordering, neighbor set, harness prompt mismatch, candidate-set
> instability, or a combination — and whether isolation-stability generalizes to all 14 flippers.*

**Already settled (doctrine — NOT gated by 375D-2; the 375E doctrine half can be drafted now):** directional
confirmation count is collapsed into legal severity and Risk routing (`3-0 → HIGH → Risk`), even though
agreement strength measures **verification support**, not **legal materiality**. Two-output separation
(materiality vs verification strength, routing governed from both without equating them) stands regardless of
how the stability question resolves.

## The two harnesses (NEVER blurred — candidate-gen variance vs Pass-2 verification variance)
| harness | file | what it does | output |
|---|---|---|---|
| **A — fixed-candidate Pass-2 context sensitivity** | `build_log/_375d2_trackA.py` | **Freezes** a canonical directional candidate set ONCE (Pass-1 once → `375D2_canonical_candidates.json`), then calls Role B (gpt-5.4) under **5 conditions** — single-candidate isolation / full fixed batch / fixed small batches (~5) / shuffled order (fixed permutations) / neighbor-set perturbation — **≥K repeats each**, capturing per call: candidate id+LP, batch id, batch size, **position**, **neighbor ids**, **prompt md5**, verdict, integrity/match status, and the hypothetical tally the current rule would assign. Candidate generation is **never** re-run inside Track A. | `build_log/375D2_trackA.json` + `375D2_canonical_candidates.json` |
| **B — candidate-generation / retention variance** | `build_log/_375d2_trackB.py` | Re-runs Pass-1 → `_collect_directional_candidates` → Pass-2 **N times** and classifies the six vanished LPs (**LP-03/06/11/17/18/24**) per run: `never_generated` (no Pass-1 mismatch_flag) / `generated_not_candidate` (flagged but dropped pre-Pass-2 = hidden filter) / `generated_pass2_rejected` (entered Pass-2, suppressed) / `in_final`. Kept **separate** from Track A. | `build_log/375D2_trackB.json` |

Both call the REAL functions (no reimplementation): `_collect_flagged_lps`, `_build_evaluator_user_prompt`,
`_call_single_evaluator`, `_collect_directional_candidates`, `_build_pass2_user_prompt`,
`_call_pass2_evaluator` (role B = `openai/gpt-5.4`), `_p2_build_directional_index`/`_p2_lookup_directional`,
`_build_pass2_directional_findings`. Track A includes **all 14 flippers** that are present in the frozen
canonical set (any flipper not generated by that one Pass-1 is reported under `flippers_missing` — the
candidate-set variance Track B explains), plus all in-set unanimous controls **and any genuine 2-1 split**.

## ▶ HOW TZVI RUNS THEM (keyed machine)
Keys load from `C:\Users\Owner\OneDrive\DoubleCheck\doublecheck-api\api_keys\.env` (sets `DISABLE_OPENROUTER=1`
etc.). PowerShell: paste ONE bare command per line (no trailing comments). From the repo root:

```powershell
cd "C:\Users\Owner\OneDrive\CAM"
git pull
```
**Track A — step 1: freeze the canonical candidate set (Pass-1 once):**
```powershell
python "build_log\_375d2_trackA.py" --freeze
```
**Track A — step 2: preview the call-count cost (no model calls):**
```powershell
python "build_log\_375d2_trackA.py" --dry
```
**Track A — step 3: run the 5 conditions (the big one — size with the knobs if needed):**
```powershell
python "build_log\_375d2_trackA.py"
python "build_log\_375d2_trackA.py" --k 6 --perms 2 --neighbor-groups 2 --neighbor-repeats 3
```
**Track B — candidate-generation audit (cheaper):**
```powershell
python "build_log\_375d2_trackB.py"
python "build_log\_375d2_trackB.py" 8
```
**Commit the emitted JSON:**
```powershell
git add -f "build_log\375D2_trackA.json" "build_log\375D2_trackB.json" "build_log\375D2_canonical_candidates.json"
git commit -m "Step 375D-2: emitted Role-B context-sensitivity replay artifacts"
git push origin main
```

## Cost (Track A prints an exact estimate before the condition loop)
≈ `K*targets (isolation) + K (full) + K*ceil(N/5) (small) + PERMS*K (shuffle) + NB_GROUPS*NB_REPEATS*flippers
(neighbor)` Role-B calls. With defaults (K=10, PERMS=3, neighbor 2×5) and ~18 targets / 26 canonical this is
**~900–1000 gpt-5.4 calls** — run `--dry` first; lower with the knobs. Track B ≈ N×6 calls (default 30).

## Dry validation Code performed (no model calls)
- `py_compile` clean on both harnesses.
- All 11 imported symbols present; `_build_pass2_user_prompt`, `_call_pass2_evaluator`,
  `_collect_directional_candidates`, `_build_pass2_directional_findings` signatures match the call sites;
  `_EVALUATOR_LINEUP_PASS2["B"]` = `openai/gpt-5.4` with all required keys; Pass-1/Pass-2 roles A/B/C.
- The deterministic shuffle helper (`_perm`) yields valid distinct permutations (no RNG — reproducible).
- Frozen input present in 52adbf (`full_tenant_text`, `coverage_assessment`, `conflicts`); candidate dicts
  carry `candidate_id` + `lp_ids`, and `_build_pass2_user_prompt` keys directional candidates by
  `candidate_id` — so batch/order/neighbor manipulation matches verdicts cleanly.

## Output the analysis must produce (after Tzvi runs them)
1. Per-candidate stability table across the 5 Track-A conditions (in `375D2_trackA.json.per_candidate_verdict_by_condition`).
2. Whether isolation-stability **generalizes from the original 5 flippers to all 14**.
3. Determination: does batch **size / order / neighbor-set / prompt-mismatch** drive Role-B variance — or
   mixed/unresolved (allowed; report honestly).
4. **Separate** explanation for the pass-4 candidate-count loss (Track B `fate_tally_per_target`).
5. Recommended implementation **options only, no production change**: per-candidate Pass-2 / bounded
   small-batch / prompt-isolation / stable candidate-generation / unresolved-if-mixed.

## Constraints honored
- READ-ONLY w.r.t. production; calls models but changes no code/output. Pinned to the 375D baseline (run
  52adbf, current synthesis code). **Code did NOT execute them.** 375G is frontend-only and does not touch
  Stage 7 — it does not contaminate this experiment.
- External demo / Joshua use of Stage-7 directional Risk totals remains **PAUSED**.

## Decisions Needed
1. Tzvi runs Track A (`--freeze` → `--dry` to size → run) and Track B, commits the JSON.
2. Then analyze → choose the stability implementation strategy. (The two-output doctrine half of 375E can be
   drafted in parallel — it is not gated by this.)
