# Step 465 — Instruction

**Received:** 2026-08-23, verbatim from Tzvi in conversation, transcribed to disk before execution
per CLAUDE.md Reporting Integrity Rule 7.

---

Step 465. Extraction union — DIAGNOSTIC, no pipeline change.

Using the 18 persisted extraction runs, compute the union offline. Do not
make new calls.

For each pair and each triple of runs drawn from different shapes:
  - does the union contain §13.2 in LP-12?
  - does it contain "shall mean 22.4%" anywhere?
  - does it contain both?
  - how many of the 4 evidence-varying provisions (LP-00, LP-02, LP-12,
    LP-17) are covered versus any single run?

Then: what is the smallest N that gets both clauses with high probability,
given the observed shape frequencies A 5/18, B 12/18, C 1/18?

Also report what union would MEAN operationally — do provisions merge by
concatenation, or does one shape's tenant_text replace another's? State the
ambiguity rather than resolving it.

Report. Change nothing.
