Step 560. LP-20's applicability. DESIGN, no build.

Three real leases abort on LP-20 alone: everbridge (negation), ncino
(enumeration item), albireo (correctly-quoted third-party covenant).
Step 535's negation rule addresses one of three.

The matcher decides an LP is applicable from a document-level substring
with no notion of whose obligation the text describes. The panel HAS that
notion -- Step 537 measured it returning 0 of 7 on ex6-4's twenty-three
pages of other tenants' exclusives, and Step 559 shows albireo is the same
shape.

1. What is applicability FOR? Report where its output is consumed. If its
   only effect is deciding whether an empty extraction aborts the run,
   say so -- that reframes the fix.

2. Given the panel demonstrably outperforms the matcher on the same
   phrase in the same document, is the matcher the right layer for this
   question at all? Consider: let the LP through, let the panel judge, and
   let an empty extraction on a genuinely-applicable LP abort as it does
   now. Report what breaks -- Step 494's LP-17 was a genuine extraction
   failure the gate correctly caught, and that must survive.

3. If the matcher stays, what would fix all three causes rather than one?
   Negation handles everbridge. Enumeration and third-party attribution
   are different problems. Say honestly whether a substring matcher can
   answer "whose obligation is this" at all.

4. Step 559's albireo now provides the true positives Step 535 lacked --
   real percentage rent, real termination option. Report what a negation
   rule would do to those, since the falsification base finally exists.

5. What is the cheapest thing that unblocks three real leases? It may not
   be the most correct thing. Say which is which.

Do NOT build. Do NOT change any clue list.
