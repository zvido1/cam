# Step 520 — Instruction

**Received:** 2026-08-31, verbatim from Tzvi in conversation, transcribed to disk before
execution per CLAUDE.md Reporting Integrity Rule 7.

---

Step 520. LP-07 applicability survey. DIAGNOSTIC, no change.

LP-07 is applicability_mode "always" — hardcoded required, no clue list,
no mechanism to be wrong. Divall is a ground lease with zero CAM or
proportionate-share language and LP-07 is required on it.

Step 495's method, applied here:

1. Across every fixture that parses, does the document actually have a
   CAM / operating-expense / proportionate-share provision? Establish
   ground truth by READING, not by a substring proxy — Step 495's
   automated proxy misclassified three of thirty-two in both directions.

2. How many fixtures would a conditional LP-07 need to keep firing on?
   Report the positive count before proposing any clue set, so a narrowed
   list is measured against what it must not lose.

3. Propose candidate clue sets and report each against the survey: TP/FP/
   TN/FN, which fixtures flip, and whether each flip is correct. Reject any
   clue that fires on nothing — Rule 1, per Step 495's rejection of C3/C4.

4. State plainly what a conditional LP-07 costs. It is one of the most
   consequential LPs in the schema; a false not_applicable on a lease that
   DOES have CAM is a silent all-clear on the tenant's largest variable
   cost. Step 481's LP-12 clue change made divall unprocessable — a change
   here has more downside.

5. Is `conditional` even the right answer, or is there a mode between
   always and conditional? Report what the schema supports.

Do NOT implement. Report the survey and your recommendation.
